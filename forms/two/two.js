/*
Do the same program as #1 but using 
controls and events (no prompts, alerts, etc.).
*/

// Code
/*
const dice = {
sides: 32,
roll() {
    return Math.floor(this.sides * Math.random()) + 1;
}
}

let randomNumber1 = dice.roll()
let randomNumber2 = dice.roll()
let sum = randomNumber1+randomNumber2

console.log(`${sum}`)
*/